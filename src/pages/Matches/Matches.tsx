import styles from '../../styles/utility.module.scss'

const Matches: React.FunctionComponent = () => {
  return (
    <h2 className={styles.headerTemp}>Your matches</h2>
  )
}

export default Matches
